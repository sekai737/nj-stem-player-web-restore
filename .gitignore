node_modules
dist
dist-electron
release
.electron-library-path
fonts-source/*.ttf
fonts-source/*.otf
fonts-source/*.TTF
fonts-source/*.OTF
.DS_Store
*.local
.env
.env.*
!.env.example

# Removed LRC builder (manual ChatGPT workflow)
lrc-builder/

# Stem audio — distributed via torrent, not bundled in git
public/stems/
public/stems.zip

build/bundled-stem-library-installer/

# Stem library installer sub-project
stem-library-installer/release
