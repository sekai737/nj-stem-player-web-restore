# MIDI downloads

Place one MIDI file per song here and reference it in `src/data/catalog.json`:

```json
"midi": "/midi/supernatural.mid"
```

Supported extensions: `.mid`, `.midi`

The song menu **Download MIDI (.mid)** item is enabled only when a song has a `midi` path.
